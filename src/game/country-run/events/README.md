# events/

Reserved for Country Run's actual events content (instances of the generic
`GameEvent` type from `src/engine/events/types.ts`).

**Empty in M0** — no event content is authored yet (e.g. the Year 1 energy
shock from Product Bible §13). That is Year 1 vertical-slice content,
explicitly out of scope for this foundation milestone.
