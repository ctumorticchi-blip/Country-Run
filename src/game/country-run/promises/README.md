# promises/

Reserved for Country Run's campaign promise library (instances of the
generic `GamePromise` type from `src/engine/state/promise.ts`).

**Empty in M0** — the promise library (Product Bible §9) is not authored
yet.
