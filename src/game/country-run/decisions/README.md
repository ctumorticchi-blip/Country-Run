# decisions/

Reserved for Country Run's actual decisions content (instances of the
generic `Decision` type from `src/engine/state/decision.ts`).

**Empty in M0** — no decision content is authored yet. That is Year 1
vertical-slice content (Product Bible §13), explicitly out of scope for
this foundation milestone.
